# Runtime dependencies installed inside the Airflow image.
#
# Notes:
#   * The framework itself is deliberately NOT installed in this
#     image — see infrastructure/airflow/Dockerfile's header comment
#     for why (SQLAlchemy 1.4 vs 2.0 conflict with Airflow itself).
#   * Versions are pinned for reproducibility; bump deliberately.

# Database access. psycopg2, not psycopg 3 — Airflow itself is built
# on psycopg2 (the base image bundles it) and the entrypoint's
# create-database step uses the same driver. Pinned explicitly rather
# than relying on the base image's transitive copy.
psycopg2-binary==2.9.10

# Experiment tracking.
mlflow==2.20.3

# Real ML training + ingestion for the demo.
xgboost==2.1.3
scikit-learn==1.6.0
pandas==2.2.3
numpy==2.1.3

# Server-side keeps httpx so any in-worker HTTP calls (e.g. talking
# to the ServingBridge) work without a separate install.
httpx==0.27.2
