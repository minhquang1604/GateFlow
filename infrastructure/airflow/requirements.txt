# Runtime dependencies installed inside the Airflow image.
#
# Notes:
#   * The framework itself is installed in editable mode via Docker
#     build context (`pip install -e .`) at the project root, so it
#     is NOT listed here.
#   * Versions are pinned for reproducibility; bump deliberately.

# Database access (matches the framework's runtime).
psycopg[binary]==3.2.3

# Experiment tracking.
mlflow==2.20.3

# Real ML training + ingestion for the demo.
xgboost==2.1.3
scikit-learn==1.6.0
pandas==2.2.3
numpy==2.1.3

# Server-side keeps httpx so any in-worker HTTP calls (e.g. talking
# to the ServingBridge) work without a separate install.
httpx==0.27.2
